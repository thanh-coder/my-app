import { BindToSecondPropertyDirective } from './bind-to-second-property.directive';

describe('BindToSecondPropertyDirective', () => {
  it('should create an instance', () => {
    const directive = new BindToSecondPropertyDirective();
    expect(directive).toBeTruthy();
  });
});
